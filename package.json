{
    "$schema": "https://json.schemastore.org/package.json",
    "private": true,
    "scripts": {
        "build": "echo 'Using CDN assets - no build required'",
        "dev": "echo 'Using CDN assets - no dev server required'"
    },
    "devDependencies": {
        "axios": "^1.8.2"
    }
}
